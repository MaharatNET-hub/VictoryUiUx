# تجربة الماسح الضوئي — التشغيل والنشر

## أولاً: الجهاز (مرة واحدة)

الأسرع والأضمن هو البلوتوث، فلا يحتاج برنامجاً ولا حاسوباً:

1. شغّل الماسح.
2. امسح باركود **Bluetooth HID Mode** من دليل الجهاز (صفحة ٧).
3. من قائمة البلوتوث في الهاتف أو الحاسوب، اقترن بجهاز **NETUM WF Scanner**.
4. الضوء الأزرق يثبت عند نجاح الاقتران.

صار الماسح لوحة مفاتيح. افتح صفحة `/scan.html` وامسح.

> إن بقيت على وضع WiFi: ألغِ تفعيل **Open the website** في برنامج Netum، فهو يفتح
> المتصفح عند كل بيانات واردة. أبقِ **Emulates a keyboard** وحده.

## ثانياً: النشر على Render

المستودع الآن خدمة Node، والاستضافة الثابتة لا تُشغّلها.

1. من لوحة Render اضغط **New +** ثم **Web Service**.
2. اختر مستودع `MaharatNET-hub/VictoryUiUx`.
3. املأ الحقول:

   | الحقل | القيمة |
   |---|---|
   | Branch | الفرع الذي فيه هذا العمل |
   | Runtime | `Node` |
   | Build Command | `npm install` |
   | Start Command | `node server.js` |
   | Instance Type | `Free` |

4. اضغط **Create Web Service** وانتظر انتهاء البناء.
5. افتح `https://<اسم-الخدمة>.onrender.com/scan.html`.

لا متغيرات بيئة مطلوبة. `PORT` يضبطه Render وحده.

## ثالثاً: العرض أمام العميل

1. افتح `/scan.html` على جهاز المسح.
2. افتح **العنوان نفسه على هاتف العميل**.
3. امسح منتجاً واربطه بصنف — مرة واحدة فقط.
4. امسحه ثانية: ينقص المخزون، ويظهر السطر على الجهازين في اللحظة ذاتها.

سجل «الدليل» يعرض لكل عملية رقماً تسلسلياً ووقتاً **من ساعة الخادم**، ويميّز
الشاشة التي أنتجتها. المتصفح لا يستطيع اختلاق رقم تسلسلي من الخادم.

## تنبيهات

- خطة Render المجانية تُنيم الخدمة بعد خمول، وإيقاظها يستغرق نحو دقيقة.
  **افتح الصفحة قبل موعد العرض بدقيقتين** حتى تكون مستيقظة.
- الحالة محفوظة في ذاكرة العملية: تُصفَّر عند إعادة تشغيل الخدمة. كافٍ للعرض،
  ويحتاج قاعدة بيانات للإنتاج.

## للتجربة محلياً

```bash
node server.js
```

ثم افتح `http://localhost:3000/scan.html`. ولتجربة جهاز ثانٍ على الشبكة نفسها،
استخدم عنوان حاسوبك المحلي بدل `localhost`.
